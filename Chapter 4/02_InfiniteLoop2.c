#include <stdio.h>

int main(void) {
    int i=0;
    while (2<10){
     printf("The value of i is %d!\n",i);
     i++;
    }   // to end the infinite code , click on terminal on that loop
    // then press ctrl + c
    return 0;
}